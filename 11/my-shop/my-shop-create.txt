nest new my-shop
cd my-shop

nest g resource categories

npm i class-validator class-transformer dotenv
npm i @prisma/client@6
npm i -D prisma@6


npx prisma init --datasource-provider sqlite

npm run start:dev

//prisma/schema.prisma 에 테이블 생성
 npx prisma migrate dev --name init //테이블 생성

//prisma nestjs 연결
nest g module prisma
nest g service prisma --no-spec



//서버 오류시 다시 생성 
npx prisma generate #client api를 만들어주고 (findall, findOne,...)

npm run start:dev

npx prisma format #schema 파일을 정렬 깨끗하게.


npx prisma generate
  # 스키마(모델 파일)을 실제 사용할 수 있는 DB 클라이언트 코드로 바꿔줍니다.
  # Js
     Model Category {
        id int @id
        name String @unique
     }
    ==> 
    @prisma/client 에 다음과 같은 API
    js
        this.prisma.category.create();
        this.prisma.category.findMany();
        ...

schema.prisma: DB구조 설계도(사람이 작성)
npx prisma migrate : DB 테이블 실제 생성
npx prisma generate : 애플리케이션 코드에 사용할 클라이언트 API(메소드)를 생성

만약 모델이 추가/삭제, 필드 추가/삭제 변경 되면
반드시 npx prisma generate 

prisma@6 을 사용하기 때문에  (schema.prisma)
 generator client { provider = "prisma-client-js" }

node_modules/.prisma/client -> @prisma/client


npx prisma studio #GUI 웹 화면 데이터 조작이 가능.



################# PRODUCTS 
nest g resource products --no-spec
>RESTAPI > y

schema.prisma 테이블 넣어주고 
npx prisma format 

#테이블 생성 #1)
npx prisma migrate dev --name add_product 

#service, controller 생성 #2)
npx prisma generate

    #schema.prisma 수정 되면  1), 2) 번 실행 - DB 반영

#create-product.dto.ts 에 컬럼 추가 
#product.moudle.ts 에 categorymodule 추가 
#products.service.ts 에 contructor 추가 
#categories.module.ts 에 export CategoriesServce 추가
#products.service.ts api수정 
   # QueryProductDto 만들기 where ? ? ? query 로 만들떄.


################# 스웨거 설치 
npm i @nestjs/swagger

//변경해야 할 파일
main.ts
controller.ts
DTO.ts            